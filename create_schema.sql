
CREATE TABLE Geek(
id numeric , 
nom varchar(20), 
prenom varchar(20),
date_naissance varchar(10),
adresse varchar(60),
ville varchar(30),
cp varchar(5),
email varchar(30),
login varchar(30),
password varchar(30),
PRIMARY KEY(id)
);

CREATE SEQUENCE SEQ START WITH 1;